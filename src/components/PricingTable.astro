---
// Pricing tier table — used on /pricing.
// Tier data centralized here so updates land in one place.

interface Tier {
  name: string;
  devices: string;
  monthly: string;
  annual: string;
  highlight?: boolean;
}

// Annual values match what's configured in Stripe Products & Prices and
// what's listed on docs/pricing.md (15% off the monthly × 12 calculation).
// Keeping the display as the absolute annual total (rather than the
// monthly-equivalent) so customers see the same dollar figure on the
// website, on the docs pricing page, and on the Stripe Checkout page.
const tiers: Tier[] = [
  { name: 'Starter',      devices: '100',    monthly: '$399',   annual: '$4,070/yr (save 15%)'  },
  { name: 'Professional', devices: '250',    monthly: '$899',   annual: '$9,170/yr (save 15%)', highlight: true },
  { name: 'Business',     devices: '500',    monthly: '$1,599', annual: '$16,310/yr (save 15%)' },
  { name: 'Enterprise',   devices: '1,000',  monthly: '$2,799', annual: '$28,550/yr (save 15%)' },
  { name: 'Scale',        devices: '1,000+', monthly: 'custom', annual: 'multi-site, multi-tenant pricing' },
];
---

<div class="grid grid-cols-1 gap-6 md:grid-cols-2 lg:grid-cols-5">
  {
    tiers.map((tier) => (
      <div
        class={`relative rounded-2xl border p-6 ${
          tier.highlight
            ? 'border-brand-600 bg-brand-50 shadow-lg ring-1 ring-brand-600'
            : 'border-slate-200 bg-white'
        }`}
      >
        {tier.highlight && (
          <span class="absolute -top-3 left-1/2 -translate-x-1/2 rounded-full bg-brand-600 px-3 py-1 text-xs font-semibold uppercase tracking-wider text-white">
            Most popular
          </span>
        )}

        <h3 class="text-lg font-semibold text-slate-900">{tier.name}</h3>

        <div class="mt-4">
          <p class="text-3xl font-bold tracking-tight text-slate-900">{tier.monthly}</p>
          <p class="mt-1 text-xs text-slate-500">{tier.annual}</p>
        </div>

        <div class="mt-6 border-t border-slate-200 pt-4">
          <p class="text-sm text-slate-700">
            <span class="font-semibold">{tier.devices}</span>{' '}
            fully-monitored devices
          </p>
          <p class="mt-1 text-sm text-slate-500">
            Unlimited ICMP-only
          </p>
        </div>

        <div class="mt-6">
          {tier.name === 'Scale' ? (
            <a
              href="/contact?topic=scale"
              class={`block rounded-lg px-4 py-2 text-center text-sm font-semibold no-underline transition-colors ${
                tier.highlight
                  ? 'bg-brand-600 text-white hover:bg-brand-700'
                  : 'border border-slate-300 bg-white text-slate-700 hover:bg-slate-50'
              }`}
            >
              Contact sales
            </a>
          ) : (
            <a
              href="/download"
              class={`block rounded-lg px-4 py-2 text-center text-sm font-semibold no-underline transition-colors ${
                tier.highlight
                  ? 'bg-brand-600 text-white hover:bg-brand-700'
                  : 'border border-slate-300 bg-white text-slate-700 hover:bg-slate-50'
              }`}
            >
              Start free trial
            </a>
          )}
        </div>
      </div>
    ))
  }
</div>
